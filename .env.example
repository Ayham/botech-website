# ============================================================
# BOTech Website — Environment Variables (frontend / public)
# ============================================================
# These are PUBLIC (non-secret) values usable from the browser.
# NEVER put service-role keys, access tokens, or database
# passwords in any VITE_* variable.
#
# Copy this file to .env.local for local development.
# .env / .env.*.local are already gitignored.

# --- BOTech Main (company backend, hosts the admin console) ---
VITE_SUPABASE_URL=https://mxkechnqkcyxbgbwvriz.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_KFH2p26ZU_ACcRAeGFzL2g_-9Q_Yw0g

# Edge Functions base path (same host as SUPABASE_URL, default /functions/v1)
VITE_SUPABASE_EDGE_FUNCTIONS_URL=

# --- Raseed (public anon key, only used for any public reads) ---
VITE_RASEED_SUPABASE_URL=https://tebyyidgcsivzslaohxd.supabase.co
VITE_RASEED_SUPABASE_ANON_KEY=

# --- Clover (public anon key, only used for any public reads) ---
VITE_CLOVER_SUPABASE_URL=https://heamecscymivebbnzzda.supabase.co
VITE_CLOVER_SUPABASE_ANON_KEY=